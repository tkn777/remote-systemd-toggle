[Definition]

failregex =
    remote-systemd-toggled.*tls handshake failed from <HOST>:
    remote-systemd-toggled.*client certificate missing from <HOST>
    remote-systemd-toggled.*client certificate CN mismatch from <HOST>:

ignoreregex =
